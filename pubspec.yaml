name: keep_keyboard_popup_menu
description: A popup menu that will keep keyboard open.
version: 1.0.0
author:
homepage:

environment:
  sdk: ">=2.12.0-0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:


flutter:
